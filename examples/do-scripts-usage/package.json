{
  "private": true,
  "name": "do-scripts-usage",
  "type": "module",
  "workspaces": [
    "do"
  ],
  "scripts": {
    "start": "clear; npm -w do run start",
    "stage": "clear; npm -w do run stage",
    "build": "clear; npm -w do run build"
  },
  "dependencies": {
    "@11ty/eleventy": "^4.0.0-alpha.8",
    "@anyblades/eleventy-blades": "^1.2.2"
  }
}